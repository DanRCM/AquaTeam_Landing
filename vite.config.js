import { defineConfig } from 'vite'

export default defineConfig({
  // IMPORTANTE: Asegúrate que este nombre sea IDÉNTICO al de tu repo en GitHub
  // Si tu repo se llama 'AquaTeam_Landing', déjalo así. 
  // Fíjate en las mayúsculas y minúsculas.
  base: '/AquaTeam_Landing/', 
})